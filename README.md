# new-one

Next.js 15 starter generated by GAPT!

## Quick start

```bash
git clone https://github.com/CocoRoF/new-one.git
cd new-one
docker compose up -d --build
```

* App: http://localhost:3000

Local dev (no Docker):

```bash
cd new-one
npm install
npm run dev
```
